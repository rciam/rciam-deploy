# {{ ansible_managed }}

voms = {
    "vomses_url": "{{ rciam_utils_sync_voms.vomses_url }}",
    "cert_path": "{{ rciam_utils_sync_voms.ssl.cert.path }}",
    "key_path": "{{ rciam_utils_sync_voms.ssl.cert_key.path }}",
    "trusted_ca_path": "{{ rciam_utils_sync_voms.ssl.trusted_ca_path }}"
}

registry = {
    "db": {
        "name": "{{ rciam_dbs.registry.name }}",
        "host": "{{ lookup('dig', groups['dbmaster'][0]) }}",
        "user": "{{ rciam_dbs.registry.owner_username }}",
        "password": "{{ rciam_dbs.registry.owner_password }}"
    }
}
